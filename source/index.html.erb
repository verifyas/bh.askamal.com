<% set_meta_tags title: data.site.name +
  " | " + data.site.description,
  full_title: false %>

<div class="flex-col w-full h-full justify-between bg-teal-300 flex" style="background: linear-gradient(180deg, #3E40F8 0%, #3537F8 100%);">
  <div class="font-bold text-3xl text-white text-center mx-4 my-2">
    Still guessing where<br>
    it’s all going?
  </div>
  <div id="target" class="flex-1 flex justify-around overflow-x-hidden relative" style="width: 300vw;">
    <img src="/images/1.png" class="object-contain">
    <img src="/images/2.png" class="object-contain">
    <img src="/images/1.png" class="object-contain">
  </div>
  <div class="text-2xl text-center text-white font-bold my-2">
    Amal magically breaks <br>
    down your spending
  </div>
  <div class="rounded-full bg-green-400 py-2 mx-8 mt-2 mb-4 font-bold text-green-700 text-xl text-center ">
    get early access
  </div>
</div>

<script>
  var initialX = null;
  var offsetX = 0;

  function downHandler(ev) {
    console.info("down", ev.clientX);
    let el = document.getElementById("target");
    initialX = ev.clientX;
    el.onpointermove = moveHandler;
  }

  function moveHandler(ev) {
    console.info("move", ev.clientX);
    let el = document.getElementById("target");
    el.style.left = offsetX - (initialX - ev.clientX) + 'px';
  }

  function cancelHandler(ev) {
    console.info("cancel", ev.clientX);
    let el = document.getElementById("target");
    offsetX += -(initialX - ev.clientX);
    el.onpointermove = null;
  }

  (function() {
    // onload
    let el = document.getElementById("target");
    // Register pointerdown and pointercancel handlers
    el.onpointerdown = downHandler;
    el.onpointercancel = cancelHandler;
    el.onpointerup = cancelHandler;
  })();
</script>